package Scaling

import ColorSpaces.ColorSpaceInstance
import Configurations.ImageConfiguration

class Lanczos3Algorithm {
    companion object {
        fun getNewImage(
            pixels : Array<ColorSpaceInstance>,
            originalWidth : Int,
            originalHeight : Int,
            newWidth : Int,
            newHeight : Int) : ImageConfiguration {
            TODO("Create implementation")
        }
    }
}