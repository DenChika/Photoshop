package Scaling

import ColorSpaces.ColorSpaceInstance
import Configurations.ImageConfiguration

class BCSplinesAlgorithm {
    companion object {
        fun getNewImage(
            pixels : Array<ColorSpaceInstance>,
            originalWidth : Int,
            originalHeight : Int,
            newWidth : Int,
            newHeight : Int,
            ) : ImageConfiguration {
            TODO("Create implementation and add parameters because what type do u need")
        }
    }
}